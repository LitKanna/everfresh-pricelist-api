services:
  - type: web
    name: everfresh-pricelist-api
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.19.0
